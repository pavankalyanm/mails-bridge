[project]
name = "gmail-mcp-server"
version = "0.2.0"
description = "Multi-account Gmail MCP server for Claude Code (IMAP/SMTP + App Passwords)"
readme = "README.md"
requires-python = ">=3.11"
dependencies = [
    "mcp[cli]>=1.27.2",
]
